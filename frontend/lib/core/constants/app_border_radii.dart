import 'package:flutter/material.dart';

class AppRadii {
  AppRadii._();

  static const BorderRadius rounded = BorderRadius.all(Radius.circular(20));

  static const BorderRadius small = rounded;
  static const BorderRadius medium = rounded;
  static const BorderRadius large = rounded;
  static const BorderRadius extraLarge = rounded;
  static const BorderRadius pill = rounded;
  static const BorderRadius topExtraLarge = rounded;
}
